const { 
    Engine, 
    Render, 
    Runner, 
    World, 
    Bodies,
    Body,
    Events
} = Matter;

// Default Generation
mazeGeneration(15,10);

// User customization
// const refreshButton = document.querySelector('.refresh') 
// refreshButton.addEventListener('click', () => {
//     const numberCellsHorizontal = parseInt(document.querySelector('.cellsHorizontalInput').value);
//     const numberCellsVertical = parseInt(document.querySelector('.cellsVerticalInput').value);
//     console.log(render.canvas)
//     mazeGeneration(numberCellsHorizontal,numberCellsVertical)
    

// })



